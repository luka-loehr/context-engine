# API keys (set only the one you use)
GOOGLE_API_KEY=
XAI_API_KEY=

# Optional: Ollama local server configuration
# Change if your Ollama server runs elsewhere

# Optional: HTTP proxy settings
# HTTP_PROXY=
# HTTPS_PROXY=
# NO_PROXY=localhost,127.0.0.1

# Optional: defaults (CLI also stores selection internally)
# PROMPTX_PROVIDER=google
# PROMPTX_MODEL=promptx
